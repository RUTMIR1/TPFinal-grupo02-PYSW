name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Configurar Node.js y Angular
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Cambia esto a la versión que necesites

      # 3. Instalar dependencias de Angular
      - name: Install dependencies
        run: |
          npm install

      # 4. Ejecutar pruebas de Angular
      - name: Run tests
        run: |
          npm run test -- --watch=false --browsers=ChromeHeadless

      # 5. Verificar si las pruebas pasaron
      - name: Check if tests passed
        if: success()  # Solo continua si las pruebas pasaron
        run: echo "Tests passed, proceeding to Docker build"

      # 6. Construir la aplicación de Angular
      - name: Build Angular application
        run: |
          npm run build --prod

      # 7. Log in to DockerHub
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 8. Construir la imagen de Angular
      - name: Build and push Angular image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/angular-frontend:latest -f ./Dockerfile ./  # Asegúrate de que la ruta al Dockerfile esté correcta
          

      # 9. subir al docker hub
      - name: Deploy to server
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/angular-frontend:latest
        
